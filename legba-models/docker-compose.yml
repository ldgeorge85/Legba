services:
  legba-models:
    build: .
    container_name: legba-models
    restart: unless-stopped
    volumes:
      - /mnt/data/models/legba:/data
    environment:
      - NVIDIA_VISIBLE_DEVICES=0
      - HF_HOME=/data/hf-cache
      - TRANSFORMERS_CACHE=/data/hf-cache
      # Optional defense-in-depth shared secret (see .env.example). Unset =
      # no in-app auth (service is already network-internal-only + caddy-fronted).
      - LEGBA_MODELS_API_SECRET=${LEGBA_MODELS_API_SECRET:-}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0']
              capabilities: [gpu]
    networks:
      - fastchat
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"

networks:
  fastchat:
    external: true

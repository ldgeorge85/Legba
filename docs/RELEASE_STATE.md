# Release State

<!-- GENERATED by scripts/generate_release_manifest.py — do not hand-edit. -->

_GENERATED by scripts/generate_release_manifest.py — do not hand-edit. Regenerate with `PYTHONPATH=src python3 scripts/generate_release_manifest.py` after registering/retiring a source, desk, unit, or migration — every number below is a live SELECT against the registry DB (`is_head`, filtered to `state='active'` except where a row's state is reported explicitly), never hand-typed. README.md and docs/FAQ.md quote this file rather than repeating the numbers._

## Sources

- **Active source descriptors: 107**
- By kind:
  - `rss`: 99
  - `geojson`: 3
  - `json_api`: 3
  - `gdelt_files`: 1
  - `telegram_channel`: 1

## Country desks

- **Total: 32** — 19 G20 + 13 watch tier
- G20: 19
- Watch tier: 13
- `nuclear_watch`-tagged subset: 8 (the `proliferation_watch` unit's fan-out)
- Other active targets (region/situation frames, not desks, not counted above): 6 — `region_africa`, `region_americas`, `region_europe`, `region_indo_pacific`, `region_mena`, `situation_iran_war`

## Thematic desks (10 registered)

- **Active: 6** of 10 registered — the remainder are registered `draft` and fan out only once an operator activates them.

| Desk | State | Tags |
|---|---|---|
| `flow_energy_shipping` | active | `disruption`, `flow`, `supply_chain` |
| `flow_semiconductors` | active | `disruption`, `flow`, `supply_chain` |
| `lane_black_sea` | active | `disruption`, `lane`, `supply_chain` |
| `lane_hormuz` | active | `disruption`, `lane`, `supply_chain` |
| `lane_malacca_south_china_sea` | active | `disruption`, `lane`, `supply_chain` |
| `lane_red_sea` | active | `disruption`, `lane`, `supply_chain` |
| `flow_container_freight` | draft | `disruption`, `flow`, `supply_chain` |
| `flow_critical_minerals` | draft | `disruption`, `flow`, `supply_chain` |
| `lane_baltic_north_sea` | draft | `disruption`, `lane`, `supply_chain` |
| `lane_panama` | draft | `disruption`, `lane`, `supply_chain` |

## Bounded reasoning units (9)

| Unit | Name | Fan-out predicate |
|---|---|---|
| `disruption_status` | Supply-Chain Disruption-Status Unit | `has_tag("supply_chain")` |
| `economic_coercion` | Economic-Coercion / Sanctions-Trade-Currency Unit | `has_tag("g20") or has_tag("watch")` |
| `energy_security` | Energy-Security Pressure Unit | `has_tag("g20") or has_tag("watch")` |
| `escalation` | Escalation Risk Unit | `has_tag("g20") or has_tag("watch")` |
| `internal_stability` | Internal-Stability / Coup-Risk Unit | `has_tag("g20") or has_tag("watch")` |
| `leadership_transition` | Leadership-Transition Risk Unit | `has_tag("g20") or has_tag("watch")` |
| `military_posture` | Military-Posture Shift Unit | `has_tag("g20") or has_tag("watch")` |
| `narrative_coordination` | Narrative / Coordination Signals Unit | `has_tag("g20") or has_tag("watch")` |
| `proliferation_watch` | Proliferation-Watch / WMD-Risk Unit | `has_tag("nuclear_watch")` |

- Country-desk-scoped: 8 · domain-scoped (thematic desks): 1 — `disruption_status`

## Composition layers (5)

| Layer | Name |
|---|---|
| `country_composition` | Per-Country Composition (G20 units → one country read) |
| `escalation_composition` | Global Escalation Composition (escalation-unit heads across all desks → one read) |
| `escalation_dyad` | IR–IL Escalation Dyad (escalation heads of Iran + Israel → one dyad read) |
| `region_composition` | Per-Region Composition (country reads → one region read) |
| `world_assessor` | World Situational-Awareness Composition |

## Schema

- Migration head: `0118_strip_telegram_widget_archived_text.sql`
- Applied migrations: 84

## Verify configuration

- Active analysts carrying a `method.llm.verify` pass: 24
- Judge route: SAME-MODEL (every verify-bearing analyst's `method.llm.verify.raw` equals its own `method.llm.primary.raw`)
- Judge stack component(s) in use: `llm.primary.openai_compat`
- Registered but NOT in the active verify route: `llm.anthropic.opus_4_7`, `llm.judge.cerebras_gemma4_31b.openai_compat`, `llm.judge.nemotron3_super.openai_compat`, `llm.judge.nemotron3_ultra.openai_compat`, `llm.verify.slm_8b`

## Experimental / optional features (derived)

- Tier-1 (structured) knowledge grounding enabled: 13 active analysts, including 9/9 bounded units (`disruption_status`, `economic_coercion`, `energy_security`, `escalation`, `internal_stability`, `leadership_transition`, `military_posture`, `narrative_coordination`, `proliferation_watch`)
- Tier-2 (vector RAG) grounding pilot: `internal_stability` — `vector:world_context`
- `LEGBA_COMPOSITION_VERIFY_FLOOR` (composition-layer verify floor (default 0.0 = accept any faithfulness score; see meta_findings_synthesizer.DEFAULT_VERIFY_FLOOR)): unset (default applies)
- `LEGBA_WORLD_CONTEXT_DISABLED_UNITS` (per-run Tier-2 RAG auto-rollback kill-switch (comma-list of unit ids force-disabled; see src/legba/runtime/rag_rollback.py)): unset (default applies)

_Env-flag values above reflect the environment THIS SCRIPT ran in — if generated outside the runtime container, an unset flag means "not overridden here", not necessarily "unset in production"._

## Declared (hand-maintained — NOT generated)

Everything above this section is a live SELECT against the registry DB. The following is NOT mechanically derivable from descriptor state or env flags and is maintained by the operator; treat it as narrative, not fact:

- UI panel maturity (live/preview/hidden) — see `docs/RELEASE_STATE_MATRIX.md` (generated from `registry.ts`, a separate generator).
- Declared-not-built seams and known limitations — see `docs/SEAMS.md`.
- Roadmap / planned work (e.g. a dedicated cross-family verify judge) — see `docs/STATUS.md` and `docs/DESIGN.md`.


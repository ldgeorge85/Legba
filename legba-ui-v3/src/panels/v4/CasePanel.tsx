/** v4 panel — Casework board (Excalidraw): pin signals/findings/entities + link. */
import { PanelBoundary } from './PanelBoundary'
import CaseRoom from '@/v4/case'

export default function CasePanel() {
  return (
    <PanelBoundary>
      <CaseRoom />
    </PanelBoundary>
  )
}
